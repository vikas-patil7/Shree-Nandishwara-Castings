Place the provided logo image file in this folder with the filename `snc-logo.png`.

Steps:
1. Save the image you uploaded in the project root (e.g., Downloads) as `snc-logo.png`.
2. Move or copy it into `public/assets/` so the path is `public/assets/snc-logo.png`.
3. Restart the dev server if running.

Note: The app now references `/assets/snc-logo.png` in `src/App.tsx`.
